一、
1、查看远程分支 git branch -r
2、新建远程分支 git push origin mybranch
3、拉取远程分支 git checkout -b dev origin/deve
3、

二、
1、从远程拉取： git clone https://github.com/PeterCuiy/PeterCuiy.github.io
1、从远程分支合并到本地主分支：git pull orgin master
1、新建本地分支： git checkout -b myBranch
1、新增文件： git add 文件名 ；git commit -m "备注"
2、合并本地master分支： git checkout master;git merge mybrance
3、提交本地master到远程orgin： git push origin master

三、
1、创建分支：git checkout -b myBranch
2、新增文件： git add 文件名 ；git commit -m "备注"
3、推送分支到远程：git push origin mybranch 
4、合并分支mybranch 到master：
5、合并到master到origin
